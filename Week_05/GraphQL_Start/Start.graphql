query {
  hello
}

query {
  users{
    id
    name
    points
  }
}

query {
  user(id: 1) {
    name
    email
  }
}

mutation {
  addPoints(userId: "1", amount: 50) {
    id
    name
    points
  }
}

